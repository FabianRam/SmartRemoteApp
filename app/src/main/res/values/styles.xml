<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/darkBlue</item>
        <item name="colorPrimaryDark">@color/darkBlue</item>
        <item name="colorAccent">@color/cherryRed</item>
        <item name="android:textColorPrimary">@color/yellowBrown</item>
        <item name="android:textColorSecondary">@color/white</item>

        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
        <item name="android:windowSharedElementEnterTransition">@android:transition/move</item>
        <item name="android:windowSharedElementExitTransition">@android:transition/move</item>
        <item name="android:actionBarItemBackground">@drawable/logo_remote</item>

        <item name="android:typeface">serif</item>
    </style>


    <style name="AppTheme.AppBarOverlay"  >
    </style>

    <style name="Theme.AppCompat.Light.DarkActionBar"  >
    </style>

    <style name="TabLayout">
        <item name="android:textColor">@color/white</item>
    </style>

    <!--- change spinner item!-->
    <style name="SpinnerTheme" parent="android:Widget.Spinner">
        <item name="android:background">@color/cherryRed</item>
        <item name="android:dropDownSelector">@drawable/not_chosen_bubble</item>
    </style>

    <style name="SpinnerTheme.DropDown">
        <item name="android:spinnerMode">dropdown</item>
    </style>


    <!-- Changes the spinner drop down item radio button style -->
    <style name="DropDownItemSpinnerTheme" parent="android:Widget.DropDownItem.Spinner">
        <item name="android:checkMark">@drawable/not_chosen_bubble</item>
    </style>

    <style name="ListViewSpinnerTheme" parent="android:Widget.ListView">
    </style>

    <style name="ListViewSpinnerTheme.White" parent="android:Widget.ListView.White">
        <item name="android:listSelector">@drawable/not_chosen_bubble</item>
    </style>

    <style name="SpinnerItemTheme"
        parent="android:TextAppearance.Widget.TextView.SpinnerItem">
        <item name="android:textColor">@color/white</item>
    </style>

    <!---switch style -->
    <style name="ColorSwitchStyle">
        <item name="colorControlActivated">@color/cherryRed</item>
        <!-- Inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/brighterBlue</item>
        <!-- Inactive track color(30% transparency) -->
        <item name="android:colorForeground">@color/orange</item>

        <item name="colorControlNormal">@color/brighterBlue</item>
        <item name="colorButtonNormal">@color/orange</item>

        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">true</item>
    </style>


</resources>
